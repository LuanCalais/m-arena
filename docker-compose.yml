services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    volumes:
      - meme_data:/app/data
    environment:
      - NODE_ENV=production
    restart: unless-stopped

volumes:
  meme_data:
